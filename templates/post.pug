extends post-base.pug

block content
    header(style="text-align:center; padding-bottom: 20px; border-bottom: 5px solid grey; margin-bottom: 30px;")
        h1(style="")
            !=meta.title
        time(style="color: grey;")
            !=meta.date
    section(class="articles")
        !=content
